<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anylogic.ssystem.bms.com.mapper.ComUserinfoDetail2Mapper">

    <!-- 개인전자서명 삭제 -->
    <delete id="deleteUserSecretgrade" parameterType="com.anylogic.ssystem.bms.com.model.DeleteUserDetailReqVO">
        DELETE FROM sbms.sbms_user.com_userinfo_detail2
        WHERE 1 = 1
            and userid = #{userid}
    </delete>

    <!-- 개인전자서명 등록 -->
    <insert id="insertUserSecretgrade" parameterType="com.anylogic.ssystem.bms.com.model.InsertUserDetailReqVO">
        INSERT INTO
        sbms.sbms_user.com_userinfo_detail2(
        <trim suffixOverrides=",">
            <if test="userid != null and userid !=''">
                userid,
            </if>
            <if test="secretgrade != null and secretgrade !=''">
                secretgrade,
            </if>
            <if test="inid != null and inid !=''">
                inid,
            </if>
            <if test="indt != null and indt !=''">
                indt,
            </if>
        </trim>
        )
        VALUES(
        <trim suffixOverrides=",">
            <if test="userid != null and userid !=''">
                #{userid},
            </if>
            <if test="secretgrade != null and secretgrade !=''">
                #{secretgrade},
            </if>
            <if test="inid != null and inid !=''">
                #{inid},
            </if>
            <if test="indt != null and indt !=''">
                #{indt},
            </if>
        </trim>
        )
    </insert>

    <!-- 개인전자서명 수정 -->
    <update id="updateUserSecretgrade" parameterType="com.anylogic.ssystem.bms.com.model.UpdateUserDetailReqVO">
        UPDATE sbms.sbms_user.com_userinfo_detail2
        <trim prefix="SET" suffixOverrides=",">
            <if test="userid != null and userid !=''">
                userid = #{userid},
            </if>
            <if test="secretgrade != null and secretgrade !=''">
                secretgrade = #{secretgrade},
            </if>
            <if test="upid != null and upid !=''">
                upid = #{upid},
            </if>
            <if test="updt != null and updt !=''">
                updt = #{updt},
            </if>
        </trim>
        WHERE 1 = 1
            and userid = #{userid}
    </update>


</mapper>
